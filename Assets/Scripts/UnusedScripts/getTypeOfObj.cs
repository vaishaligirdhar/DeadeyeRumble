using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class getTypeOfObj : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Debug.Log(gameObject.GetType ().ToString ());
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
